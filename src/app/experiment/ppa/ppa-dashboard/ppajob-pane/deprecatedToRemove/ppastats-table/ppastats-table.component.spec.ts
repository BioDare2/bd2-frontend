/*
import {PPAStatsTableComponent} from './ppastats-table.component';

describe('PPAStatsTableComponent', () => {
  let component: PPAStatsTableComponent;
  let fixture: ComponentFixture<PPAStatsTableComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [PPAStatsTableComponent]
    })
      .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PPAStatsTableComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
