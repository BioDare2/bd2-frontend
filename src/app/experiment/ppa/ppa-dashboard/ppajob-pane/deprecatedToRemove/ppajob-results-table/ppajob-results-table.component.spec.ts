import {async, ComponentFixture, TestBed} from '@angular/core/testing';

/*
import {PPAJobResultsTableComponent} from './ppajob-results-table.component';

describe('PPAJobResultsTableComponent', () => {
  let component: PPAJobResultsTableComponent;
  let fixture: ComponentFixture<PPAJobResultsTableComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [PPAJobResultsTableComponent]
    })
      .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PPAJobResultsTableComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
*/
