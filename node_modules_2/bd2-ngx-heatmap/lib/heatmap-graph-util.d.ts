import { GraphicContext, LookAndFeelSizing, Serie } from './bd2-heatmap.dom';
export declare class HeatmapGraphUtil {
    prepareGraphicContext(data: Serie[], lookAndFeel: LookAndFeelSizing, middleZero?: boolean): GraphicContext;
    calculateDimensions(context: GraphicContext, data: any[], lookAndFeel: LookAndFeelSizing): void;
    calculateWorkspaceHeight(data: any[], lookAndFeel: LookAndFeelSizing): number;
    addPaneAttributes(context: GraphicContext, lookAndFeel: LookAndFeelSizing): void;
    addScales(context: GraphicContext, data: Serie[], lookAndFeel: LookAndFeelSizing, middleZero: boolean): void;
    addFormatters(context: GraphicContext, data: Serie[]): void;
    timeMargin(data: Serie[], timeDomain: [number, number]): number;
    timeDomain(data: Serie[]): [number, number];
    heatmapScale(data: Serie[], middleZero: boolean): import("d3-scale").ScaleQuantize<string, never>;
    valuesRange(traces: Serie[]): [number, number];
    formatForDomain([min, max]: [number, number]): (n: number | {
        valueOf(): number;
    }) => string;
    labelsColors(traces: Serie[]): (i: number) => string;
}
